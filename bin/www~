#!/usr/bin/env node
var debug = require('debug')('duke_gardens');
var app = require('../app');

app.set('port', 8080);

var server = app.listen(8080, function() {
  console.log('Express server listening on port ' + server.address().port);
});
